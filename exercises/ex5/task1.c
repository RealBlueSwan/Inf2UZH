#include <stdio.h>
#include <stdlib.h>

int ontrolMaxHeap(int *array, int currentIndex, int length) {
    return 0;
}

int main(){
    int array[] = {5, 5, 3, 2, 1, 4};
    int length = sizeof(array) / sizeof(array[0]);
    int solution = ontrolMaxHeap(array, 0, length);
    printf("solution: %d\n", solution);
    return 0;
}